#!/bin/bash

# Execute the program and capture its output
output=$(./crackme3)

# Extract the password from the output
password=$(echo "$output" | tail -c 5)

# Save the password to a file
echo "$password" > 101-password
